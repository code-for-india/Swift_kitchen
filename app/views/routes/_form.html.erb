<%= form_for(@route, :html => {:class => "form-horizontal"} ) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, class: "control-label" %><br>
    <%= f.text_field :name, class:"form-control" %>
  </div>
  <div class="field">
    <%= f.label :route_code, class: "control-label" %><br>
    <%= f.text_field :route_code , class:"form-control" %>
  </div>
  <div class="field">
    <%= f.label :kitchen_id, class: "control-label"  %><br>
    <%= f.text_field :kitchen_id , class:"form-control" %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
